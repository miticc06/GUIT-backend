{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts", "src/**/*.schema.ts"],
  "exec": "node ./node_modules/.bin/ts-node -r tsconfig-paths/register src/main.ts",
  "events": {
    "start": "node -e 'console.clear(); console.log(`❖ - Starting in dev mode...`)'"
  }
}
