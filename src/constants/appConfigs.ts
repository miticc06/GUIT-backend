const DEFAULT_PORT: number = 4000
const DEFAULT_MONGODB_URL: string = 'localhost:27017/dbname'

const DEFAULT_SECRET_KEY = 'abcdefgh'
const GRAPHQL_DEPTH_LIMIT: number = 6
const GRAPHQL_PATH: string = '/graphql'
const SCHEMA_PATH: string = '/schema'
const TOKEN: string = 'token'
const SALT_ROUNDS: number = 12

export const _appConfigs = {
  DEFAULT_PORT,
  DEFAULT_MONGODB_URL,
  DEFAULT_SECRET_KEY,
  GRAPHQL_DEPTH_LIMIT,
  GRAPHQL_PATH,
  SCHEMA_PATH,
  TOKEN,
  SALT_ROUNDS
}
